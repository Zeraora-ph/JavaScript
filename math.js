let x = 3.14;
let y = 5;
let z = 9;
let maximum;
let minimum;

// x = Math.round(x);
  // Round

// c = Math.floor(x);
 // Round Down

// x = Math.ceil(x);
  // Round Up

// x = Math.pow(x, 3);
  // Power 

// x = Math.sqrt(x);
  // Give's the SquareRoot of a number

// x = Math.abs(x);
  // Find an absolute value

//maximum = Math.max(x, y, z);
//minimum = Math.min(x, y, z);
  // Show the maximum or the minimum number of the variables

x = Math.PI;
  // Displays the number of PI
console.log(x);